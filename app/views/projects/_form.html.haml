= form_for @project do |f|

  = f.label :name
  = f.text_field :name
  
  = f.label :email
  = f.text_field :email

  = f.label :logo
  = f.text_field :logo

  = f.label :misc_image
  = f.text_field :misc_image
  
  .actions.form-actions
    = f.submit "Save", class: "btn btn-large btn-primary"
